#include "AuthWindow.h"
#include <QVBoxLayout>
#include <QLabel>
#include <QPushButton>

AuthWindow::AuthWindow(QWidget *parent)
    : QWidget(parent)
{
    resize(1600, 1000);
}

AuthWindow::~AuthWindow()
{
}
